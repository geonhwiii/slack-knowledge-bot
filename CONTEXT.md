# Context

슬랙 스레드에서 오간 논의를 검색 가능한 지식으로 축적하고,
축적된 지식을 근거로 질문에 답하는 봇.

이 저장소는 특정 회사에 속하지 않는 범용 봇이다. 조직 고유의 값(봇 이름, 채널,
프로젝트 키, 사내 용어)은 코드가 아니라 설정과 환경변수로 들어간다.

## Glossary

### Knowledge Entry

지식베이스에 저장되는 한 건. 슬랙 스레드에서 LLM이 추출한 **구조화된 레코드**이며,
항상 원본 스레드로 되돌아갈 수 있는 참조(permalink)를 함께 가진다.

원문 전체도 아니고, 산문 요약 한 덩어리도 아니다. 검색되는 부분(증상)과
답변이 되는 부분(해결책)이 필드로 분리되어 있다는 점이 핵심이다.

사람이 필드를 채우지 않는다. 사람은 저장을 지시할 뿐이고, 추출은 전적으로 봇의 일이다.

**스레드 하나가 Entry 하나다.** 스레드가 곧 Entry의 정체성이며, 같은 스레드에 저장을
다시 지시하면 새 Entry가 생기는 게 아니라 스레드를 다시 읽어 기존 Entry를 갱신한다.
"헷갈리면 다시 저장해줘 하면 된다"가 사용자가 기억해야 할 유일한 규칙이다.

### Situation

Knowledge Entry에서 **검색이 비교하는 필드**. "무엇이 문제였는가 / 무엇을 정하려 했는가".

`kind: issue`에서는 증상이지만, `decision`이나 `howto`에서는 증상이라 부를 것이 없어
중립적인 이름을 쓴다. 어느 쪽이든 "나중에 같은 상황을 만난 사람이 검색창에 쓸 법한 말"이
들어가는 자리라는 점은 같다.

해결책(`resolution`)과 분리되어 있는 것이 핵심이다. "이 이슈 원래 있던 거야?"는
상황 대 상황을 비교해야 맞는 질문이고, 해결책이나 잡담과 비교하면 안 된다.

### Visibility

Knowledge Entry에는 가시성 구분이 없다. **저장된 Entry는 워크스페이스 전원이 검색할 수 있다.**
출처가 비공개 채널이어도 마찬가지다.

봇은 초대된 채널에서만 동작하므로 초대가 곧 읽기 위임이고, 저장은 명시적 지시로만
일어난다. 즉 저장을 지시한 사람이 공개를 선택한 것으로 본다. 그래서 저장 완료 응답은
전사 검색 대상이 됨을 항상 알린다.

공개해선 안 될 것이 들어갔을 때의 수단은 가시성 조절이 아니라 **삭제**다.

### Status

Knowledge Entry가 담은 논의가 결론에 도달했는지 여부. `resolved` / `unresolved`.

미해결도 저장한다. "작년에도 같은 증상이 있었고 그때도 원인을 못 찾았다"는
가장 값진 답변 중 하나이기 때문이다. 해결한 것만 기억하는 지식베이스는 반쪽이다.

### Kind

Knowledge Entry의 종류. 별도 타입이 아니라 하나의 Entry가 가지는 구분값이다.

- **issue** — 무언가 잘못됐고, 그에 대한 논의
- **decision** — 무언가를 이렇게 하기로 정함
- **howto** — 이건 원래 이렇게 하는 것이다 (운영 지식)

경계는 모호하다. 장애 논의 끝에 결정이 나는 스레드가 흔하다. 그래서 Kind는
저장 위치를 가르는 기준이 아니라, 검색 결과를 설명할 때 쓰는 꼬리표에 가깝다.

### Issue (금지어)

단독으로 쓰지 않는다. 아래 셋을 구분해서 부른다.

- **Knowledge Entry (kind: issue)** — 지식베이스에 쌓인 과거 문제 기록
- **Jira Issue** — Jira에 존재하는 티켓
- **논의 주제** — 아직 아무데도 기록되지 않은, 지금 스레드에서 오가는 이야기

"이 이슈 원래 있던 이슈야?"라는 질문은 정확히는
"지금 이 **논의 주제**와 증상이 겹치는 **Knowledge Entry**가 있는가?"이다.

## Decisions

### 범위와 이전

- 개인 슬랙 워크스페이스에서 먼저 구현하고, 조직에는 **코드만** 이전한다. 데이터는 옮기지 않는다.
- 이 저장소는 MIT로 공개한다. 조직에서는 fork가 아니라 **복제**(`clone --bare` + `push --mirror`)로
  비공개 사본을 만든다. GitHub은 공개 저장소의 fork를 비공개로 만들 수 없기 때문이다.
- **동기화는 단방향이다.** 공개 저장소 → 조직 저장소로만 당겨온다. 조직 저장소의 변경을
  공개 저장소로 올리지 않는다. 조직 쪽 코드에는 사내 용어가 프롬프트와 설정에 스며들기 때문에,
  역방향 PR은 그 자체가 유출이다.
- Jira / GitHub 연동은 1차 범위 밖. 단, Knowledge Entry의 구조가 그대로
  Jira 티켓 필드로 매핑되도록 설계해 확장 비용을 낮춘다.

### 동작

- 저장 트리거는 명시적 멘션 하나뿐이다. 이모지 리액션·채널 단위 자동 저장은
  넣지 않는다. 다만 저장 동작 자체는 진입점과 분리해, 나중에 트리거를 추가할 때
  진입점만 늘어나도록 한다.
- 삭제는 1차 범위다. 저장이 있으면 삭제가 있어야 짝이 맞고, 전 공개 정책의
  유일한 사고 대응 수단이기 때문이다.
- DM에서는 저장하지 않는다. 요약·검색 등 읽기 기능만 쓴다.
- 검색은 **정밀도 우선**이다. 확실할 때만 "관련 있다"고 말한다. 봇이 단정하는 부분과
  "참고하라"고 보여주는 부분은 응답에서 분리하고, 단정하는 자리에는 애매한 말을 섞지 않는다.
- 검색이 실패하면(관련 기록 없음) 그 자체를 새 지식의 신호로 보고 저장을 제안한다.
- 한 스레드에서 Entry를 여러 개 뽑지 않는다(주제가 섞인 스레드는 뭉개지는 대로 둔다).
  검색이 실제로 나빠진다는 증거가 나오면 재검토한다.
- 기술검토는 판정하지 않고 **질문한다**. 코드베이스를 보지 못하는 봇의 판정에는 근거가 없다.
  대신 사내 선례를 검색해 근거로 삼고, 확인이 필요한 지점을 제시한다.

### 구현

- 도메인 로직(추출·검색·저장·검토)은 슬랙과 프레임워크를 모르는 순수 함수로 둔다.
  `bot.ts`의 핸들러는 그것을 호출해 스레드에 뿌리는 얇은 껍데기다. 런타임을 갈아도
  껍데기만 다시 쓰면 되고, 테스트도 슬랙 없이 된다.
- 지식 검색은 **하이브리드**다. pgvector 임베딩 검색과 Postgres 키워드 검색을 함께 돌려
  후보를 모은 뒤, LLM이 후보를 실제로 읽고 관련 여부를 판정한다. 벡터만으로는 에러 코드와
  고유명사를 놓치고, 키워드만으로는 같은 뜻의 다른 표현을 놓친다.
- LLM은 `claude-opus-5`를 Vercel AI SDK를 통해 호출한다. AI SDK의 스트림을
  `thread.post()`에 그대로 넘기면 슬랙 네이티브 스트리밍으로 렌더링되기 때문이다.
- 임베딩은 OpenAI `text-embedding-3-large`를 **1536차원**으로 받는다. 기본 3072차원은
  pgvector 인덱스의 2000차원 한계를 넘어 인덱스를 걸 수 없다.
- Entry는 `embedding_model`을 함께 저장한다. 임베딩 모델을 바꾸면 벡터 공간이 달라져
  전량 재생성이 필요하므로, 어느 벡터가 옛 모델의 것인지 구분할 수 있어야 한다.
- 요약은 도구로 만들지 않는다. 도구가 요약하고 에이전트가 그것을 다시 문장으로 옮기면
  모델이 두 번 돌고 결과는 한 번 걸러진 만큼 나빠진다. 에이전트에게 원문을 읽는 도구를
  주고 직접 요약하게 한다. 답변이 그대로 스트리밍되는 것도 이 방식의 이점이다.
- 배포는 Vercel + Postgres로 시작한다. 장기적으로는 다른 런타임으로 옮길 수 있고,
  그 비용을 낮추는 장치가 위의 도메인 로직 분리다.
